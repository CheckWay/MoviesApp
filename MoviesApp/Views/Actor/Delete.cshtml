@model MoviesApp.ViewModels.Actor.DeleteActorViewModel

@{ 
    ViewData["Title"] = "Удаление";
}

<h1>Удаление</h1>

<h3>Вы уверены что хотите удалить этого актера?</h3>
<div>
    <h4>Актер</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            <label asp-for="Name"></label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Name
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="Surname"></label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Surname
        </dd>
        <dt class = "col-sm-2">
            <label asp-for="Birth"></label>
        </dt>
        <dd class = "col-sm-10">
            @Model.Birth.ToString("dd.MM.yyyy")
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Вернуться к списку актеров</a>
    </form>
</div>